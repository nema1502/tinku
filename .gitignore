node_modules/
dist/
*.log
.DS_Store

# Vercel CLI state and the env file it pulls down.
.vercel

# Secrets. The negation must stay last: the Vercel CLI appends a broad `.env*`
# rule, which would otherwise swallow the example file everyone needs.
.env
.env.*
!.env.example
